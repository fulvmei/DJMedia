project.ext {
    //Release
    releaseVersion = '1.0.0-alpha01'
    releaseVersionCode = 1

    //Project
    compileSdkVersion = 33
    minSdkVersion = 21
    targetSdkVersion = 33

    //Media
    mediaVersion = 'develop-SNAPSHOT'

    //androidx
    androidxAnnotationVersion = '1.3.0'
    androidxTestJUnitVersion='1.1.5'
    androidxTestEspressoVersion='3.5.1'

    //junit
    junitVersion='4.13.2'

    modulePrefix = ':'
    if (gradle.ext.has('djmediaModulePrefix')) {
        modulePrefix += gradle.ext.djmediaModulePrefix
    }
}